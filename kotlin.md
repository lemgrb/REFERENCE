## Tech Experts

* @abreslav

- `val` to declare variable
- Using a variable inside string `println("${variable} is good"`
- `fun` : function
	```Kotlin
	fun printBorder() {}
	```
- Pass argument
	```Kotlin
	fun(border: String) {
	}
	```
- `print` Print
- `println` Print line
- `repeat(2){}` Loop
